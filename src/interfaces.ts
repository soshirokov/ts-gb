export interface ISearchFormData { 
  'city': string,
  'coordinates': [number, number],
  'check-in-date': Date,
  'check-out-date': Date,
  'max-price': number,
}

export interface IPlaces { 
  place?: ''
}
